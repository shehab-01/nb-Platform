// Template "campaign2" — Bazar Campaign Gold: the campaign pictures on a deep
// green ground with gold accents, a pulsing order button and sections that
// reveal as the visitor scrolls. Same six replaceable pictures as "campaign";
// the order form, tracking and autosave are the classic page's underneath.
import { Closed } from "@/templates/classic/closed";
import { Storefront } from "@/templates/campaign2/storefront";

export { Closed, Storefront };
