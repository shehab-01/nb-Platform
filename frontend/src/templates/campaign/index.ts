// Template "campaign": Bazar Classic with a campaign block on top — banner,
// how-to-participate, CTA, prizes, product showcase, offer price — every
// picture replaceable per store.
import { Closed } from "@/templates/classic/closed";
import { Storefront } from "@/templates/campaign/storefront";

export { Closed, Storefront };
